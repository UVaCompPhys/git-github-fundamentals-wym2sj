#!/usr/bin/env python3
import os
import math
import math
import matplotlib.pyplot as plt

for n in range(1,51):
    for 
        vol = ((math.pi**(n/2.0)))/(math.gamma(n/2)))*R**(n)
    
    
bottom = math.gamma([rad2])
top = math.pi**(rad/2.0)

#need to grab power function and to print it

filename=os.path.basename(__file__)
